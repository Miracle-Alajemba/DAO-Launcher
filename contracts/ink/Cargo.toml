[package]
name = "builders_dao"
version = "0.1.0"
authors = ["BuildersDAO Team"]
edition = "2021"

[dependencies]
ink = { version = "5.1.1", default-features = false }

[lib]
name = "builders_dao"
path = "lib.rs"

[features]
default = ["std"]
std = [
  "ink/std",
]
ink-as-dependency = []
